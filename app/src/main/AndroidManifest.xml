<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="edu.orangecoastcollege.cs273.vnguyen629.occlostandfound">

    <!--
         The ACCESS_COARSE/FINE_LOCATION permissions are not required to use
         Google Maps Android API v2, but you must specify either coarse or fine
         location permissions for the 'MyLocation' functionality. 
    -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.SEND_SMS" />
    <uses-permission android:name="android.permission.READ_CONTACTS" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".SplashScreenActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".MainMenuActivity" />
        <activity
            android:name=".ItemsListActivity"
            android:exported="true" />
        <activity android:name=".ItemDetailsActivity" />
        <activity android:name=".AboutActivity" />
        <activity android:name=".ReportItemActivity" />
        <activity android:name=".CampusMapActivity" />
        <activity android:name=".ContactInfoActivity" />
        <activity android:name=".LoginActivity" />
        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/. 
        -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_key" />

        <activity android:name=".MapActivity" />
        <activity android:name=".AdminMainMenuActivity" />
        <activity android:name=".UserItemsListActivity" />
        <activity android:name=".UserItemDetailsActivity" />
        <activity android:name=".UserMenuActivity" />
        <activity android:name=".FeedbackActivity" />
        <activity android:name=".AdminItemListActivity" />
        <activity android:name=".SendSMSActivity" />
        <activity android:name=".UserListActivity" />
        <activity android:name=".UserAccountCreateActivity" />
        <activity android:name=".PasswordChangeActivity" />
        <activity android:name=".AccountInfoActivity" />
        <activity android:name=".AdminViewUserActivity" />
        <activity android:name=".ViewUserFeedbackActivity" />
        <activity android:name=".ViewAllFeedbacksActivity" />
        <activity android:name=".ResourceSelectionActivity"></activity>
    </application>

</manifest>